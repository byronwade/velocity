{
  "name": "velocity-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Velocity — a browser-native, agent-native workspace. Monorepo: the React app (apps/web, also the Tauri desktop frontend) and the marketing site (apps/site).",
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "check": "turbo run check"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
