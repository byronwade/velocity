{
  "name": "velocity-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Velocity — a fast, tiny, native AI code editor. Monorepo: native desktop app (apps/desktop), the original web app (apps/web), marketing site (apps/site), and shared native UI (packages/ui-native).",
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "check": "turbo run check"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
